<% page_articles.each_with_index do |article, i| %>
  <div class="post frontpage">
      <span class="linkdate"><%= article.date.strftime('%d %b %Y') %> »</span>
      <%= link_to article.title, article %>
  </div>
<% end %>
